ANTHROPIC_API_KEY=            # Same as OpenClaw config — used for provider detection only
GEMINI_API_KEY=               # Same as OpenClaw config — used for provider detection only
MOONSHOT_API_KEY=             # Same as OpenClaw config — used for provider detection only
OPENCLAW_LOG_PATH=~/.openclaw/logs/openclaw.log
SYNC_INTERVAL_HOURS=6
