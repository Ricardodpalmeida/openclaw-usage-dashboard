# ── Provider detection (same keys already in ~/.openclaw/.env) ──────────────
ANTHROPIC_API_KEY=          # Used to detect Anthropic is configured
GEMINI_API_KEY=             # Used to detect Google Gemini is configured
MOONSHOT_API_KEY=           # Used to detect Moonshot/Kimi is configured

# ── Data source ──────────────────────────────────────────────────────────────
# Path to the OpenClaw session files directory.
# In Docker this is bind-mounted; locally it resolves via expanduser.
OPENCLAW_SESSIONS_PATH=~/.openclaw/agents/main/sessions

# ── Sync schedule ────────────────────────────────────────────────────────────
SYNC_INTERVAL_HOURS=6

# ── WhatsApp alerts via OpenClaw Gateway (no LLM — direct HTTP) ─────────────
# Copy OPENCLAW_GATEWAY_TOKEN from ~/.openclaw/.env on the host.
OPENCLAW_GATEWAY_URL=http://host.docker.internal:18789
OPENCLAW_GATEWAY_TOKEN=
WHATSAPP_ALERT_TO=+351910298749
